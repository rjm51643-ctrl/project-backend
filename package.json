{
  "name": "plumbline-backend",
  "version": "1.0.0",
  "description": "Backend proxy for Plumbline portfolio analytics — hides data provider keys, caches lookups, computes proprietary confidence scores.",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3"
  }
}
